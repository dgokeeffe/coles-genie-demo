resources:
  jobs:
    scrape_fsanz_recalls:
      name: scrape-fsanz-recalls-${bundle.target}
      tasks:
        - task_key: scrape
          notebook_task:
            notebook_path: ../src/jobs/scrape_fsanz_recalls.py
            base_parameters:
              catalog: ${var.catalog}
              schema: ${var.schema}
          environment_key: default
      environments:
        - environment_key: default
          spec:
            client: "1"
